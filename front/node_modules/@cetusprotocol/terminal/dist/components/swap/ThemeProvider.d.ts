import { ThemeType, WidgetTheme } from '../../types';
declare const ThemeProvider: React.FC<{
    children: React.ReactNode;
    theme?: WidgetTheme;
    themeType?: ThemeType;
}>;
export default ThemeProvider;
