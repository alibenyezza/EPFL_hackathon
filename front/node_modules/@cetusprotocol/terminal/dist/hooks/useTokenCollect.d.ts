import { Token } from '../types';
export default function useTokenCollect(): {
    handleCollectToken: (value: Token) => void;
    isCollectToken: (token: Token) => boolean;
    collectTokenMap: Map<string, Token>;
};
