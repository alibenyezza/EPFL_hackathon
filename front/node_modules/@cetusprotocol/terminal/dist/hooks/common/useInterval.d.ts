type IntervalHookOptions = {
    interval: number;
    callback: (timestamp: number) => void;
};
export declare function useInterval({ interval, callback }: IntervalHookOptions): {
    startTimer: () => void;
    stopTimer: () => void;
};
export {};
